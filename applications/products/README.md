# Products Service

## Overview
This service maintains the product catalog, category data, item listings, and stock levels.

## Development Setup
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn src.main:app --port 8002
```
