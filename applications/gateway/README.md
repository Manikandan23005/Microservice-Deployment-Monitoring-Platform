# Gateway Service

## Overview
This is the API gateway routing microservice. It serves as the primary ingress routing system, translating API namespace paths into targeted cluster service endpoints.

## Development Setup
Install dependencies:
```bash
npm install
```

Start the gateway proxy:
```bash
npm start
```
