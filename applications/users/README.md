# Users Service

## Overview
This service manages user accounts, registration, profile details, and shipping address records.

## Development Setup
Refer to the parent applications/ README.md or the main INSTALLATION.md.
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn src.main:app --port 8001
```
