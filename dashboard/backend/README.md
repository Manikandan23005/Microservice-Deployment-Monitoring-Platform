# Dashboard Backend API

## Overview
This is the API backend codebase serving endpoints for the DevOps Nexus portal.

## Frameworks
* Python FastAPI or Go (Gin/Fiber)

## Development Setup
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```
